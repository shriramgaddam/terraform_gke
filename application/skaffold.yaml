apiVersion: skaffold/v2beta12
kind: Config
metadata:
  name: application
build:
  artifacts:
  - image: simple-app
    docker:
      dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
    - deployment.yaml
    - service.yaml
